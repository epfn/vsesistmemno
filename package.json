{
  "name": "astro-template",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run sass-build && astro check && astro build && npm run format",
    "preview": "astro preview",
    "astro": "astro",
    "sass-watch": "sass --watch public/scss/style.scss public/css/style.css",
    "sass-build": "sass public/scss/style.scss public/css/style.css",
    "format": "npx prettier build --write --log-level silent"
  },
  "dependencies": {
    "@astrojs/check": "^0.7.0",
    "astro": "^4.10.0",
    "astro-relative-links": "^0.3.9",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "prettier": "3.3.1",
    "sass": "^1.77.4",
    "vite-plugin-full-reload": "^1.1.0",
    "vite-plugin-run": "^0.5.2"
  }
}
